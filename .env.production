# Disable ESLint plugin during production build
# This prevents build failures from ESLint warnings in CI environments
DISABLE_ESLINT_PLUGIN=true

# Generate source maps (optional, set to false for smaller builds)
GENERATE_SOURCEMAP=false
